version: "3.8"

services:
  mongodb:
    image: mongo:7
    container_name: adotapet-mongo
    ports:
      - "27017:27017"
    volumes:
      - adotapet_mongo_data:/data/db

volumes:
  adotapet_mongo_data:
